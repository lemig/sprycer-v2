[project]
name = "sprycer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1,<5.2",
    "psycopg[binary]>=3.2",
    "pgvector>=0.3",
    "python-decouple>=3.8",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-django>=4.9",
]
